[project]
name = "ocr_kul"
version = "0.1.0"
description = "OCR project for KUL internship"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
packages = [{ include = "ocr_kul", from = "src" }]

[dependency-groups]
dev = [
    "pyright>=1.1.407",
]

